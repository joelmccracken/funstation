#+title: Readme



* tasks
** TODO document current properties
** TODO port all other properties from shell version
#+begin_example
joelmccracken@glamdring> rg 'ws_prop_.*fix.*\(\)'
lib/properties/nix.bash
19:ws_prop_nix_daemon_installed_fix() {
86:ws_prop_nix_global_config_fix () {
140:ws_prop_nix_home_manager_fix() {

lib/properties/secrets.bash
14:ws_prop_bitwarden_secrets_fix() {

lib/properties/dotfiles.bash
43:ws_prop_df_dotfiles_fix() {

lib/properties.bash
70:ws_prop_check_mac_cli_tools_fix () {
84:ws_prop_check_mac_homebrew_installed_fix() {
96:ws_prop_check_mac_git_installed_fix() {
108:ws_prop_check_linux_git_installed_fix() {
131:ws_prop_check_workstation_dir_fix() {
157:ws_prop_check_workstation_repo_fix() {
183:ws_prop_dotfiles_git_track_fix() {
215:ws_prop_config_exists_fix() {
323:ws_prop_current_settings_symlink_fix() {
361:ws_prop_homebrew_bundle_fix() {
#+end_example
- ones still need to do
  - brew bundle
  - bitwarden secrets
  - nix home manager
** TODO develop "interactive" properties
- cli flag to enable/disable
** TODO create more verbose interaction support
- maybe add wrapper around shh commands to print them, maybe run bash -x?
** TODO add make commands
- build within nix develop shell
- build without nix shell (do the whole `nix build..`)
** TODO add github actions
- build static versions
- build dynamic versions first
** TODO add windows support (perhaps?)
** TODO add sudo nopasswd support somehow?
- maybe first run/setup will just need the normal irritation of being prompted
  but can optionally run command before ws sync is run in the future?
** TODO capture/record the setup of this vmware fusion vm
* task description of
#+begin_src md
  Add support to the NixDaemonP to ensure the `/etc/nix/nix.conf` file is correct.

  - The config for NixDaemonP should have a "system config" parameter.
    - This should be decoded as raw text, and treated as the desired contents for the nix config file.
  - do the checking and modification of the nix config after the nix installation is done.
  - if a change is made to the config file, the nix daemon must be restarted for it to be picked up.





  support providing sudo password via `-A` and the `SUDO_ASKPASS` env var.

  feature to provide a sudo password via cli from the "SUDO_ASKPASS env var, see
  https://stackoverflow.com/questions/61919931/how-to-pass-password-to-sudo-from-environment-variable-without-prompt-appearing
  maybe set all this up in the run-in-vm file? have some way to tell it to use -A for sudo?



  add support to the dotfiles so that if the current user does not have permissions to modify a given DEST file, the tool will perform the change with sudo.

#+end_src

